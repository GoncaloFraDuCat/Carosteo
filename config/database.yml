default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: localhost

development:
  <<: *default
  database: carolsteo_development

test:
  <<: *default
  database: carolsteo_test

production:
  <<: *default
  database: carolsteo_production